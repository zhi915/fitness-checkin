/* ============================================================
   云端配置（腾讯云开发 CloudBase）—— 唯一需要你手动填写的地方
   ------------------------------------------------------------
   什么都不填 = 「纯本地模式」：App 的所有功能照旧，只是没有登录与组队。
   填好 envId = 自动开启「设备账号（匿名登录）+ 组队房间」。
   ------------------------------------------------------------
   怎么拿到 envId：
     1. 打开 https://tcb.cloud.tencent.com 用微信/QQ 登录（需个人实名认证）
     2. 购买页选择「免费体验版」套餐，创建环境（选 上海 地域 + PostgreSQL 数据库）
        · 免费：3000 资源点/月，有效期 6 个月，到期前可续
        · 一个账号只能开 1 个免费环境
     3. 环境创建完成后，在控制台「环境概览」里能看到 环境 ID，形如 cloud1-3gxxxxxxxx
        → 填到下面的 envId
     4. 控制台 → 数据库 → SQL 编辑器，把 cloudbase/schema.sql 全部内容粘进去执行一次
     5. 控制台 → 身份认证 → 登录方式：
        · 打开「匿名登录」（不开的话匿名登录会直接报错）
        · 打开「邮箱验证码」并配好邮件模板（用于把设备账号升级为正式账号）
     6. 控制台 → 身份认证 → 开发设置：把你的站点域名加入「安全来源」白名单
        （GitHub Pages 就是 https://你的用户名.github.io）
        —— 这一步最容易漏，漏了前端请求会被判为非法来源。
   ------------------------------------------------------------
   关于 accessKey（Publishable Key，非必填）：
     它是设计给前端公开使用的低权限密钥，仅用于「尚未登录时以匿名身份访问」。
     本 App 的所有数据操作都发生在登录之后，所以留空也能正常工作。
     想填的话：控制台 → 身份认证 → 开发设置，复制 Publishable Key。
     （真正的权限由数据库的 RLS 行级安全策略把关，前端放什么 key 都不越权）
   ------------------------------------------------------------
   注意：留空时组队页会显示「未配置」指引，不会报错、不影响打卡。
   ============================================================ */
window.FIT_CLOUD_CONFIG = {
  envId: "sports-d6gxbwqzaaabf04d5",  // CloudBase 环境 ID（也可直接粘完整域名，会被自动归一化）
  accessKey: ""                        // 可选，Publishable Key；留空即可
};
